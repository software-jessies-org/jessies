#
# Imakefile for terminator
#
       INCLUDES = -I$(TOP)
        DEPLIBS = $(DEPXLIB)
LOCAL_LIBRARIES = $(XLIB)
        DEFINES = -DSHAPE
           SRCS = terminator.c
           OBJS = ${SRCS:.c=.o}

ComplexProgramTarget(terminator)
