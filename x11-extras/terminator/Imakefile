#
# Imakefile for x11-reaper
#
       INCLUDES = -I$(TOP)
        DEPLIBS = $(DEPXLIB)
LOCAL_LIBRARIES = $(XLIB)
        DEFINES = -DSHAPE
           SRCS = x11-reaper.c
           OBJS = ${SRCS:.c=.o}

ComplexProgramTargetNoMan(x11-reaper)
