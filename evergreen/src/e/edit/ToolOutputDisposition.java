package e.edit;

/**
 * Represents the different things that can happen to the output of a ShellCommand.
 */
public enum ToolOutputDisposition {
    CLIPBOARD,
    CREATE_NEW_DOCUMENT,
    DIALOG,
    DISCARD,
    ERRORS_WINDOW,
    INSERT,
    REPLACE;
}
